BRUTE-
 int maxProduct(vector<int>& nums) {
        int maxp=INT_MIN;
        for(int i=0;i<nums.size();i++){
            int p=1;
            for(int j=i;j<nums.size();j++){
                p*=nums[j];
                maxp=max(maxp,p);
            }
           
        }
        return maxp;
    }
