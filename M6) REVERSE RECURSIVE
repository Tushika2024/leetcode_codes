    void reverse(ListNode* &head,ListNode *temp,ListNode *prev){   TC=O(N)  SC=O(N)
        if(temp->next==NULL){
            temp->next=prev;
            head=temp;
            return;
        }
        reverse(head,temp->next,temp);
        temp->next=prev;
        return;
    }
    ListNode* reverseList(ListNode* head) {
        if(head ==NULL || head->next ==NULL){
            return head;
        }
        ListNode *temp=head;
        reverse(head,temp,NULL);
        return head;
    }
